{
  "name": "condition-pulse",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate:questions": "node scripts/validate-questions.js",
    "validate:assets": "node scripts/validate-assets.js",
    "check": "npm run validate:questions && npm run validate:assets && npm test"
  }
}
